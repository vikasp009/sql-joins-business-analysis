# SQL Joins – Business Analysis Project

## 📌 Project Overview
This project demonstrates the use of SQL JOIN operations (INNER JOIN and LEFT JOIN) to answer real-world business questions using an e-commerce dataset.

The goal is to show how relational tables can be combined to generate insights for sales, marketing, and product strategy.

---

## 🗂 Dataset Description
The dataset contains the following tables:

- customers: customer details and country
- orders: order-level information
- order_items: products purchased per order
- products: product names and prices
- categories: product categories

Primary and foreign keys are used to maintain data integrity.

---

## 🛠 SQL Concepts Used
- INNER JOIN
- LEFT JOIN
- GROUP BY
- Aggregate functions (SUM, COUNT)
- WHERE filters (date range, country)
- Table aliases for readability

---

## 📊 Business Questions Answered
1. Which customers have never placed an order?
2. What is the total revenue generated by each product?
3. Which product categories contribute the most revenue?
4. What are the sales in a specific region within a given date range?

---

## 📁 Files in This Repository
- `joins_queries.sql` – All SQL queries with comments
- `joined_output.csv` – Final joined dataset exported from MySQL
- `insights.txt` – Key business insights derived from the analysis

---

## 🚀 Outcome
This project helps build a strong foundation in SQL joins and demonstrates how SQL is used in real business analysis scenarios.

